写分治算法题时的一些感悟
1.写代码时顺序就是从原问题出发向子问题写，所以在思考时，先找出子问题是什么，然后看原问题到子问题的方向
    比如longest common sentence 题，原问题时最长序列，子问题是次长序列，所以写代码时要从数组的后往前写

回溯算法感悟
回溯时就是把所有情况可以看成一棵树（看成一种特别的二叉树，根节点也不光只有一个，反正就是把所有的情况都要考虑到（以递归的方式），
然后根据题目条件进行剪枝，一种情况不对，因为是递归所以可以回来，在进行另一分支的搜索。二叉树的左右两支转移在代码中就以if-else
语句来表示。